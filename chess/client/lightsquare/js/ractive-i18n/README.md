I18n extension for RactiveJS
============================

This project provides a basic i18n extension for Ractive templates.

Instances of the extension class have access to `__` and `__n` functions for retrieving translated text.